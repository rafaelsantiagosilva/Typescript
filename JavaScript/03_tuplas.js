// Tuplas (em TS) são arrays com tipos definidos por indice
var ourTuple; // Tem que ser obrigatoriamente nessa ordem
ourTuple = [5, false, 'Codando...'];
// Tuplas readonly
var ourTupleReadOnly = [1, true, 'verdadeiro']; // Não pode alterar ou adicionar
// Indices com nomes
var GRAPH = [1, 2];
// Desestruturando tuplas
var x = GRAPH[0], y = GRAPH[1];
